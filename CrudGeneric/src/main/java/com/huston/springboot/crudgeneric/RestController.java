package com.huston.springboot.crudgeneric;

public class RestController {


    /** Only examples
     * getLocations()->service.index() - GET
     * create(object)->create(object) - POST
     * show(primaryKey)->service.show(primaryKey) - GET
     * update(object)->update(object) - PUT
     * delete(primaryKey)->delete(primaryKey) - DELETE
     * removeLocation(primaryKey)->remove(primaryKey) - PUT
     *
     */


}
